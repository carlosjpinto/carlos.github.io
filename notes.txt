write:
- what is background css
- why doctype is at the top of the html
- why having a backgroung image is not responsive since it depends on the resolution of the screen

- how to write regex in java. remember they do not contain /
